import Backbone from 'backbone';
import $ from 'jquery';

class Router extends Backbone.Router
{
    constructor()
    {
        super();
    }

    routes()
    {
        return {

        };
    }
}

export default Router;
